import { Header, SigninForm } from "@/components/mobile";

export default function Signin() {
    return (
        <div className="page">
            <Header title="로그인" />
            <SigninForm />
        </div>
    );
}
